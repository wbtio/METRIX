services:
  nextjs:
    build: .
    ports:
      - "3000:3000"
    command: npx next dev -H 0.0.0.0
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true
